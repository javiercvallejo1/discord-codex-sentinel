{
  "name": "discord-codex-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Discord bridge for local Codex app-server sessions with a bundled Codex plugin.",
  "scripts": {
    "start": "bun src/index.ts daemon start",
    "test": "bun test",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "zod": "^3.24.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.8.3"
  }
}
